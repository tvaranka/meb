[build-system]
requires = ["setuptools"]



[tool.black]
line-length = 88
target_version = ['py310']
preview = true
exclude = '''
/(
    \.eggs         # exclude a few common directories in the
  | \.git          # root of the project
  | \.mypy_cache
  | \.vscode
  | docs
)/
